<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Ceylin'e Özel Aşk Kavanozu</title>
    <link href="https://fonts.googleapis.com/css2?family=Dancing+Script:wght@600&family=Poppins:wght@300;400&display=swap" rel="stylesheet">
    <style>
        /* 1. Arka Plan: Kalp Temalı */
        body {
            margin: 0;
            padding: 0;
            height: 100vh;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            /* Arka planı basit bir CSS kalp deseniyle oluşturuyoruz */
            background-color: #fdf2f0;
            background-image: 
                radial-gradient(circle at 10px 10px, rgba(231, 76, 60, 0.05) 20px, transparent 0),
                radial-gradient(circle at 10px 10px, transparent 21px, rgba(231, 76, 60, 0.02) 22px, transparent 23px);
            background-size: 20px 20px;
            font-family: 'Poppins', sans-serif;
            overflow: hidden;
        }

        h1 {
            font-family: 'Dancing Script', cursive;
            color: #d63031;
            font-size: 3.5rem;
            margin-bottom: 50px;
            text-shadow: 2px 2px 4px rgba(0,0,0,0.1);
        }

        .container {
            position: relative;
            width: 350px; /* Biraz daha geniş */
            height: 450px; /* Biraz daha uzun */
            display: flex;
            justify-content: center;
            align-items: flex-end;
        }

        /* 2. Kavanoz Tasarımı: Daha Gerçekçi ve Cam Hissi */
        .jar-wrapper {
            position: relative;
            cursor: pointer;
            z-index: 10;
            transition: transform 0.3s ease;
        }

        .jar-wrapper:hover {
            transform: scale(1.03); /* Hover efekti daha zarif */
        }

        /* Kapak */
        .lid {
            width: 140px;
            height: 35px;
            background: linear-gradient(to right, #d35400, #a04000, #d35400); /* Kapakta metalik doku */
            border-radius: 8px;
            margin: 0 auto;
            border: 2px solid #a04000;
            box-shadow: inset 0 5px 10px rgba(255,255,255,0.2), 0 5px 15px rgba(0,0,0,0.2);
            position: relative;
        }

        /* Kapak kenarı detayı */
        .lid::before {
            content: '';
            position: absolute;
            bottom: -5px;
            left: 50%;
            transform: translateX(-50%);
            width: 120px;
            height: 5px;
            background: rgba(160, 64, 0, 0.8);
            border-radius: 5px;
        }

        /* Boyun */
        .neck {
            width: 110px;
            height: 25px;
            background: rgba(255, 255, 255, 0.2);
            margin: 0 auto;
            border-left: 2px solid rgba(255,255,255,0.6);
            border-right: 2px solid rgba(255,255,255,0.6);
            position: relative;
        }

        /* Kavanozun Boynu ve Gövdesi */
        .glass {
            width: 190px;
            height: 230px;
            background: rgba(255, 255, 255, 0.1);
            border-radius: 50px 50px 30px 30px; /* Daha oval, gerçekçi şekil */
            border: 2px solid rgba(255,255,255,0.5);
            /* Cam gibi görünmesini sağlayan karmaşık gölgeler */
            box-shadow: 
                inset -15px 0 30px rgba(0,0,0,0.03), /* Sol gölge */
                inset 15px 0 30px rgba(255,255,255,0.6), /* Sağ yansıma */
                inset 0 -20px 20px rgba(0,0,0,0.02), /* Alt gölge */
                0 20px 40px rgba(0,0,0,0.1); /* Dış gölge */
            position: relative;
            overflow: hidden;
            display: flex;
            justify-content: center;
            align-items: center;
        }

        /* Cam yansıması detayı */
        .glass::before {
            content: '';
            position: absolute;
            top: 10%;
            left: 10%;
            width: 25%;
            height: 60%;
            background: rgba(255,255,255,0.5);
            border-radius: 50%;
            filter: blur(5px); /* Yansıma detayı */
            transform: rotate(-10deg);
        }

        /* 3. İçindeki Kağıtlar: Görünür ve Yüzen */
        /* Camın içindeki kağıtları oluşturuyoruz */
        .glass .paper-particles {
            position: absolute;
            width: 100%;
            height: 100%;
            pointer-events: none;
        }

        /* Rastgele yüzen kağıt tanecikleri */
        .glass .paper-particles::after {
            content: '✉ ✉ ✉ ✉ ✉ ✉ ✉';
            font-size: 1.2rem;
            color: rgba(243, 156, 18